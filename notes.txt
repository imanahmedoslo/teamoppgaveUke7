Ting å gjøre:

grabItem:

- Man kan grabbe item så mange ganger man vil, vi må fikse det






Switch case sin syntax:

switch(expression) {
  case x:
    // code block
    break;
  case y:
    // code block
    break;
  default:
    // code block
}
